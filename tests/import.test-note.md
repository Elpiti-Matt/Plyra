Import regressions are covered in core.test.mjs and ui.test.mjs. The emulator
does not implement a native file picker; real file picking and repeat imports
remain in docs/QA-MANUAL.md. Never label these DOM tests as device tests.
